# 复制为 .env.ps1 (该文件在 .gitignore 中, 不会进仓库), 填入你自己的值。
# start_all.ps1 启动时会自动 source。

# 进入密钥管理页 /admin 的口令(自定义, 设强一点)
$env:ADMIN_TOKEN = "在此填写你的管理口令"

# (可选) 仅首次迁移已有密钥到 apikeys.json 用; 之后都在 /admin 页生成/管理, 不需要此行
# $env:API_KEYS = "已有的旧密钥"
