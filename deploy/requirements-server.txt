# 服务器运行依赖(纯下载/解密服务, 不含逆向阶段工具)
# 与根 requirements.txt 的差异:
#   - 去掉 frida(服务器无设备, 签名走 unidbg SIGN_SERVER)/mitmproxy/paramiko
#   - 补 pycryptodome(解密 AES-CTR 必需, 根清单遗漏)
requests
curl_cffi       # TLS/JA3 指纹伪装(IMPERSONATE);未装则透明回退 requests
fastapi
uvicorn[standard]
pycryptodome
pillow
pillow-heif   # 仅 /img HEIF 封面代理用; 装不上可删
