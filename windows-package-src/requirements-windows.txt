# Windows 本机脱机直连 · Python 依赖(下载/串流最小集,推荐)
# 装进便携 embeddable Python:  python\python.exe -m pip install -r requirements-windows.txt
requests
fastapi
uvicorn[standard]
pycryptodome

# --- 可选:要在 /ui 里看封面缩略图(HEIC 转 JPEG)再放开下面两行 ---
# pillow
# pillow-heif

# 不需要: frida / mitmproxy / paramiko / redis (逆向/服务器阶段才用)
