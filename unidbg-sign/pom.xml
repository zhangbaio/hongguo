<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.hongguo</groupId>
    <artifactId>unidbg-sign</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <!-- 红果(fqnovel)签名脱机化:用 unidbg 在桌面 JVM 模拟跑 libmetasec_ml.so 生成 X-Argus/X-Gorgon/X-Ladon -->

    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Maven Central 的 unidbg(groupId 不带 .unidbg);本地 install 的打补丁版覆盖它 -->
        <dependency>
            <groupId>com.github.zhkl0228</groupId>
            <artifactId>unidbg-android</artifactId>
            <version>0.9.9</version>
        </dependency>
        <!-- Unicorn2 后端: 提供可在 Linux x64 运行的自洽 native(natives/linux_64/libunicorn.so);
             unidbg-android 默认声明为 test scope, 这里提为 compile 打进 fat jar 以跨平台运行 -->
        <dependency>
            <groupId>com.github.zhkl0228</groupId>
            <artifactId>unidbg-unicorn2</artifactId>
            <version>0.9.9</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>1.7.36</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <descriptorRefs><descriptorRef>jar-with-dependencies</descriptorRef></descriptorRefs>
                    <archive><manifest><mainClass>com.hongguo.sign.MetasecSign</mainClass></manifest></archive>
                </configuration>
                <executions>
                    <execution><phase>package</phase><goals><goal>single</goal></goals></execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
